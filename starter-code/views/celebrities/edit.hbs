<h2>💅 Edit your celebrity 💅</h2>
<form action="/celebrities/{{celeb.id}}" method="post">
    <label for="name">Name: </label>
    <input id="name" type="text" name="name" value="{{this.celeb.name}}">
    <br>
    <label for="occupation">Occupation: </label>
    <input id="occupation" type="text" name="occupation" value="{{this.celeb.occupation}}">
    <br>
    <label for="catchPhrase">Catchphrase: </label>
    <input id="catchPhrase" type="text" name="catchPhrase" value="{{this.celeb.catchPhrase}}">
    <label for="movie-select">Appears in: 
        <select name="movies" multiple>    
      {{#each moviesFromDB}}
        <option value="{{this._id}}"> {{this.title}} </option>
      {{/each}}
    </select>
    <p>(Hold down the Ctrl (windows) or Command (Mac) button to select multiple options.)</p>
    <br>
    <br>
    <button type="submit">Save changes 💾</button>
</form>